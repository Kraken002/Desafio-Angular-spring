import { CatergoriaPipe } from './catergoria.pipe';

describe('CatergoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new CatergoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
